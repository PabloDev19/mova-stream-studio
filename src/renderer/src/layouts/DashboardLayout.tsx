import './layout.css'

export const DashboardLayout = ({}) => {
  return (
    <section className="dashboard_layout">
      <article className="aside_menu"></article>
      <article className="render_view"></article>
    </section>
  )
}
