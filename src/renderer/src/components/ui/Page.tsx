import './page.css'

export const Title = ({ children }) => {
  return <h3 className="ui-title">{children}</h3>
}

export const SubTitle = ({ children }) => {
  return <h4 className="ui-sub_title">{children}</h4>
}
